
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <br>

        <input class="btn btn-primary col-12" type="submit" value="{{ SubmitButtonText }}" formnovalidate>
    {{ form_end(form) }}

